diff --git a/src/components/front-office/ui/ProfileForm.tsx b/src/components/front-office/ui/ProfileForm.tsx
--- a/src/components/front-office/ui/ProfileForm.tsx
+++ b/src/components/front-office/ui/ProfileForm.tsx
@@
- const payload = {
-   email: resolvedEmail,
-   ...(firstName ? { firstName } : {}),
+ const payload = {
+   ...(firstName ? { firstName } : {}),
    ...(lastName ? { lastName } : {}),
    ...(full ? { phoneNumber: full } : {}),
    ...(profile.gender ? { gender: profile.gender.toUpperCase() } : {}),
    ...(profile.ageRange ? { ageRange: profile.ageRange } : {}),
    ...(profile.localAssembly ? { localAssembly: profile.localAssembly } : {}),
    ...(profile.maritalStatus
      ? { maritalStatus: profile.maritalStatus.toUpperCase() }
      : {}),
    ...(profile.employmentStatus ? { employmentStatus } : {}),
    ...(profile.state ? { stateOfResidence: profile.state } : {}),
    ...(profile.country ? { country: profile.country } : {}),
    ...(profile.address ? { residentialAddress: profile.address } : {}),
    ...(profile.isMinister === "yes" || profile.isMinister === "no"
      ? { isMinister: profile.isMinister }
      : {}),
  };

diff --git a/src/components/front-office/ui/EventSelection.tsx b/src/components/front-office/ui/EventSelection.tsx
--- a/src/components/front-office/ui/EventSelection.tsx
+++ b/src/components/front-office/ui/EventSelection.tsx
@@
- const handleContinue = () => {
-   onContinue()
- }
+ const handleContinue = () => {
+   if (!selectedEventId) {
+     setError("Please select an event to continue.")
+     return
+   }
+   onContinue()
+ }

diff --git a/src/components/front-office/ui/EmailVerification.tsx b/src/components/front-office/ui/EmailVerification.tsx
--- a/src/components/front-office/ui/EmailVerification.tsx
+++ b/src/components/front-office/ui/EmailVerification.tsx
@@
 const res = await validateOtp(payload)

 setTokenCookie(res.token)
 setAuthToken(res.token)

+// ensure token is available before navigation
+await new Promise((r) => setTimeout(r, 50))

 onVerified(email)

diff --git a/src/components/front-office/ui/ReturningUserLogin.tsx b/src/components/front-office/ui/ReturningUserLogin.tsx
--- a/src/components/front-office/ui/ReturningUserLogin.tsx
+++ b/src/components/front-office/ui/ReturningUserLogin.tsx
@@
 const response = await generateLoginOtp(email)

+sessionStorage.setItem("otpReference", response.reference)

diff --git a/src/components/front-office/ui/Dashboard.tsx b/src/components/front-office/ui/Dashboard.tsx
--- a/src/components/front-office/ui/Dashboard.tsx
+++ b/src/components/front-office/ui/Dashboard.tsx
@@
-function Dashboard(props) {
+function Dashboard(props) {
+
+  const { activeEventId } = props

diff --git a/src/lib/api/index.ts b/src/lib/api/index.ts
--- a/src/lib/api/index.ts
+++ b/src/lib/api/index.ts
@@
- return response.data
+ return response?.data ?? response